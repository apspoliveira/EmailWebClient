<div class="usernamePassword-container" ng-controller="UsernamePasswordController">
    <label 
        class="titleLabel usernamePassword-title-password" 
        for="password">Password</label>

    <div class="margin usernamePassword-field-password">
        <input 
            data-id="password" 
            data-name="password" 
            data-value="model.password" 
            data-form="form"
	    ng-model="password"
	    ng-keyup="update()"
	    type="password"></password>
    </div>

    <br/>
    <div class="margin usernamePassword-field-password-confirm">
        <input 
            data-id="passwordc" 
            data-name="passwordc" 
            data-value="model.confirmation" 
            data-form="form" 
            data-compare="model.password"
	    ng-model="passwordc"
	    ng-keyup="update()"
	    type="password"></password>
    </div>

<!--<button class="update-button" ng-click="update()" type="button">update</button>-->
</div>
