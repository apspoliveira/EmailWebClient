angular.module('webmail.utils')
    .factory('resurrecter', ressurrecter);
function ressurrecter(authentication) {
    function isAlive() {
        if (authentication.isLoggedIn()) {
            
        }
    }

    window.addEventListener('online', isAlive);

    return { init: angular.noop };
}